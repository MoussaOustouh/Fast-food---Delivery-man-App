<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="D_Fast_food.Views.MapPage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:D_Fast_food.ViewModels"  
             xmlns:model="clr-namespace:D_Fast_food.Models.MyModels"  
             x:Name="BrowseMapPage" 
             BackgroundColor="White">


    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="checked.png" Command="{Binding ConfirmeAsDelivredCommand}"/>
        <ToolbarItem IconImageSource="phone.png" Command="{Binding PhoneCallCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>